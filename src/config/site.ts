// Shared constants. Lives here so neither the app nor the seed owns the other.
export const REPO_BASE = "https://github.com/Adossi-design/";

// Homepage grid capacity, enforced in the database layer as well as the UI.
export const FEATURED_LIMIT = 6;

// Segments in the skills tier bar on the public site.
export const TIER_SEGMENTS = 10;
